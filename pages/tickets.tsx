import React from "react";

type ticketsType = {};

export const Tickets = ({}: ticketsType) => {
  return (
    <header>
      <h2 className={"text-6xl text-purple-700 font-semibold"}>Your Tickets</h2>
    </header>
  );
};

export default Tickets;
